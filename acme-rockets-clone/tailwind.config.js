/** @type {import('tailwindcss').Config} */
export default {
	content: [
		"./build/*.html", // Watch only necessary files
	],
	theme: {
		extend: {}, // Extend Tailwind as needed
	},
	plugins: [],
};
